# Hardware bench results — 2026-09-15

Owner-run hardware bench session on the WROOM calibration/test sketch, firmware commit **`af5a409`**
(`firmware/wroom_calibration/wroom_calibration.ino` — offset-compensated HX711 calibration fix +
bench-only heater test on CH2/GPIO16). Values below are measured, not estimates.

1. **TANK_EMPTY_LEVEL = `LOW`** — CONFIRMED by owner: wet finger on the sensor flips GPIO27
   HIGH→LOW; sensor OUT of water reads LOW. TANK_EMPTY is trustworthy.
2. **RELAY_ACTIVE_LOW = `true`** — confirmed (pump `p` reacted → CH1 LOW-active, CH2 identical).
3. **HX711_SCALE_FACTOR = `1068.335`**
   (replaces the deleted `305.070f` — bake into the production WROOM sketch when that file gets
   written; do **NOT** bake into `wroom_calibration.ino`).
4. **SOIL_ADC_DRY = `4095`** · **SOIL_ADC_WET = `1964`**
5. **waterTempAddress = `28 94 6B CB 00 00 00 BF`** · **soilTempAddress = `28 83 FC C8 00 00 00 0F`**
6. **PUMP_FLOW_ML_PER_SEC = `9.706`**
   (one repeat run pending to confirm non-siphon reading; tracked as a pre-flight check, not a blocker.)
